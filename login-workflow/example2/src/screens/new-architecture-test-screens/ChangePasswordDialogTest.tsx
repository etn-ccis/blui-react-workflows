import React from 'react';
import { ChangePasswordDialogBase } from '@brightlayer-ui/react-auth-workflow';

export const ChangePasswordDialogTest = () => {
    return <ChangePasswordDialogBase open={true} onSubmit={(): void => {}} />;
};
